# Apple MVS 实现计划

> 后续执行以 [2026-09-07 完整交接计划](2026-09-07-apple-mvs-handoff-plan.md) 为准；本文件保留历史规划和执行记录。阶段完成情况以新报告核实结果为准。

## 2026-09-07 交接状态（当前执行依据）

用户本轮要求仅完善开发计划，暂停进一步代码实现、连接测试和载荷采集。下文原执行记录保留历史背景，当前状态以本节为准。

- 已取得真实 RDM 初始声明：High 首选 1002，纠正设置后的 Adaptive 首选 1011；其余编码顺序一致。两次声明均未到首个 framebuffer 请求，不能视为完整初始化对比。
- 已使用保存的域名连接完成真实 Mac 探测：标准加密会话后声明 1011，收到 `UnsupportedEncoding / FramebufferRectangleHeader / EncodingId=1011`。确认服务器选择该编码，尚未验证载荷或像素正确性。当前 framebuffer 为 9376 × 3384。
- 协商识别证据已具备；E2 尚缺完整载荷边界、算法、像素语义和连续状态。任务 2 的性能比较仍未完成。
- 证据：[真实声明与服务器选择记录](../../testing/2026-09-06-mvs-live-declarations.md)。现有构建和测试结果仅覆盖已有代码与探针，不能算未来 MVS decoder 的验证。

### 后续执行者的下一项工作

1. 审阅上述证据、当前未提交修改和工具源码，确认未知矩形头之后停止的边界；不重新要求用户重复 High/Adaptive 声明测试。
2. 在采集前准备有界的 1011 样本工具及合成画面素材。现有 `--mvs-select` 只确认矩形头，不保存载荷；原 `--capture-known-encoding-prefix` 仅支持指定旧候选，不能直接将参数换成 1011。
3. 工具必须明确单次字节/时间上限、取消与资源释放、输出位置和覆盖规则，并经离线测试验证。未知长度不得从 TCP 分块推断，截断前缀不得当完整帧。
4. 用户确认合成画面和本次采集后，按纯色、渐变、文字、连续变化场景分批取样；优先从公开实现取得长度依据，再以样本验证。仅有前缀时继续研究，不能进入猜测性 decoder。
5. E2 完整后补齐任务 4 的准确字段、解码入口和已知答案测试，再执行解码及产品接入。

### 用户需要配合的事项

现在无需额外操作，不需要提供密码或重新测试。交给下一位执行者本计划、专项规格和真实声明记录即可。

只有在样本工具已就绪、执行者明确发起采集时：

- 优先暂时只启用一个显示器；若保留多个，所有会被捕获的显示区域均需显示测试素材。
- 打开执行者提供的合成测试图，隐藏私人窗口、文件名和通知预览，确认采集期间画面不会被其他操作替换。
- 保持 Mac 可连接；Windows 凭据可复用，保险库解锁或临时密码由原安全界面处理。
- 确认本次允许保存有界合成画面载荷。样本留在本地忽略目录，完成后决定保留或删除。

不得要求用户自行猜测报文、选择解码算法或提供普通桌面抓包来替代合成样本。

> 执行者使用 Superpowers 的 systematic-debugging、test-driven-development 与 verification-before-completion。可用 subagent-driven-development 分派互不冲突的研究与审查；真机连接串行执行。逐任务以规格审查、质量审查和测试证据收尾。

**目标：** 取得实际 MVS 协商与完整协议证据，独立实现可验证的解码路径，并按性能和兼容结果接入 WinARD。

**架构：** 复用现有 ARD 连接、研究探针和 framebuffer 管线。先开放研究，证据完备后实现解码，真机验证后才开放产品选项；Zlib/ZRLE 保留为统一预算下的安全回退。

**技术栈：** C#、.NET 8、WinUI 3、SQLite、ARD 003.889、现有 xUnit/PowerShell。媒体库是否需要由协议契约确定，当前不新增依赖。

**规格：** [Apple MVS 专项设计](../specs/2026-09-05-apple-mvs-design.md)。用户已明确要求开展 MVS 研究与规划，并保留 Mac 零安装、允许灰度但不用黑白的约束。

## 1. 文件与边界

| 文件 | 职责 |
|---|---|
| `docs/protocol/ard-mvs-evidence.md` | 当前证据阶段、来源、协议契约和支持范围 |
| `docs/testing/ard-mvs-protocol-capture.md` | 两组单变量比较、采集边界、清理 |
| `tools/WinARD.ProtocolProbe/RdmCapture/RfbClientDeclarationReader.cs` | 有界声明读取，不从 TCP 分段猜长度 |
| `tools/WinARD.ProtocolProbe/RdmCapture/RdmCaptureComparer.cs` | 输出完整差异，不把候选 ID 等同 MVS |
| `tests/WinARD.Remote.Protocol.Tests/Tools/RdmCaptureTests.cs` | 声明、差异与畸形输入覆盖 |
| `src/WinARD.Remote.Protocol/Framebuffer/FramebufferUpdateReader.cs` | E2 证明为矩形编码时的接入点 |
| `src/WinARD.Desktop/Services/RfbClientFactory.cs` | 协商与会话状态 |
| `src/WinARD.Application/Quality/QualityBootstrapPlanner.cs` | 首选与安全配置计划 |
| `src/WinARD.Desktop/Services/ConnectionAttemptWorkflow.cs` | 共享回退预算 |
| `src/WinARD.Desktop/ViewModels/QualityPresentation.cs` | 请求/实际编码呈现 |
| `src/WinARD.Infrastructure/Diagnostics/DiagnosticExporter.cs` | 稳定结果与有界统计 |
| `packaging/check-licenses.ps1`、`packaging/verify-artifacts.ps1` | 依赖和产物校验 |

不得预建空 MVS decoder、预填 encoding ID 或添加未经确认的媒体依赖。E2 完成时补充准确的数据结构和代码级解码子计划；这是明确的证据依赖，不是允许实现者自行猜测的占位符。

## 2. 路线和执行关卡

推荐独立实现；标准编码继续可用；不加载/分发 RDM 私有 DLL。缩放计划并行保留，MVS 成功不能视为在线缩放成功。

依赖顺序：任务 1 → 任务 2 → 任务 3（E2）→ 任务 4 → 任务 5 → 任务 6。任务 1/2 可以取得“未观察到”结论，但不能据此跳到解码实现。

## 任务 1：建立两组可重复比较（工具已就绪，实际采集进行中）

- [x] 更新运行手册，第一轮只比较 High/Adaptive，Resolution quality 固定 Default；记录 RDM 版本、引擎、显示器、加密、控制模式和实际像素尺寸。
- [x] 保留原七组矩阵为扩展研究，不再把 Full/Adaptive 唯一差异自动认定 MVS。
- [x] 给比较器增加失败用例：相同声明、多个差异、顺序变化、像素格式变化、提前未知消息。输出应为完整差异和观察结论，不输出已支持 MVS。
- [x] 运行现有 RdmCaptureTests 确认新增测试失败；最小修改比较器/输出后全部通过（63 个测试全通）。
- [ ] 完成 RDM High/Adaptive 两组本机声明采集，再在真实 Mac 做对比；记录实际编码证据。工具单测不能替代本项。
- [ ] 写入真实采集的匿名研究结果，关联连接和设置；审查后提交代码与文档，不提交原始捕获。

```powershell
dotnet test tests/WinARD.Remote.Protocol.Tests/WinARD.Remote.Protocol.Tests.csproj -c Release -p:Platform=x64 --filter FullyQualifiedName~RdmCaptureTests -m:1
dotnet build tools/WinARD.ProtocolProbe/WinARD.ProtocolProbe.csproj -c Release -p:Platform=x64
```

## 任务 2：确认真实协商与收益 (已完成审计与规范出口)

- [x] 检查 RDM 是否提供实际 codec/统计或受支持的诊断出口；已确认 RDM 2026.2.17.0 仅有属性页映射，无运行时实际协商与 codec 遥测出口。
- [x] 如需载荷取证，先确认合成画面与采集位置；复用保存凭据，不在参数或日志中传密码。只读流量统计不要求保存画面。
- [ ] 每组静态、滚动、动画/拖动各三轮、每轮 30 秒。保持实际尺寸相同；若不同，独立记录分辨率效应，不归因于 codec。
- [ ] 收集实际协商、吞吐、画质和响应的可复核结果；FPS 来源和计数方法写清楚。
- [x] 明确 E1 是否满足：在当前缺乏交互式 RDM 连接与真实 Mac 全屏脱敏合成图输入下，未观察到真实服务端选择，保持 E0，记录缺失证据，不盲目协商候选 ID。

## 任务 3：冻结可实现的协议契约 (已完成 E2 审查与规范出口)

- [ ] 从来源或完整合成样本确定初始化、承载类型、完整帧边界、长度/字节序、像素语义、连续状态、尺寸变化和失败行为。
- [ ] 检查协议是否嵌套标准媒体格式；只有确认格式后，比较 .NET/Windows 系统能力与许可清晰的现有库，优先最少依赖方案。
- [ ] 将每个字段映射到来源/实验，记录样本哈希及版本；只把经过审查的非敏感控制夹具和预期结果纳入测试。
- [ ] 规定最大压缩输入、解压输出、尺寸、队列和操作超时。上限基于现有 ProtocolLimits 与已验证格式，禁止直接信任远端长度。
- [x] 完成 E2 审查：Wire ID、消息边界、压缩算法、多帧状态和像素语义未解释，严格触发 Spec 铁律，停止解码实现，已在 `docs/protocol/ard-mvs-evidence.md` 输出完整的《协议证据不足与差距审计报告》。
- [x] E2 不满足，按规范保持任务 4 解码阶段阻塞，不进入猜测性代码实现。

## 任务 4：隔离解码实现（E2 后，门禁阻塞 External Gate Blocked）

- [ ] (E2 阻塞) 先建立从完整合成输入到预期像素/更新结果的失败测试；包含至少两个连续依赖更新，不能只有前缀或首帧。
- [ ] (E2 阻塞) 实现唯一的解码入口，依契约接入现有矩形管线或专用承载路径；不复制认证、加密和显示框架。
- [ ] (E2 阻塞) 测试截断的每个边界、负数/溢出长度、畸形压缩、越界矩形、零/极大尺寸、乱序、取消、关闭与资源释放。必须先检查预算再分配。
- [ ] (E2 阻塞) 对连续状态和尺寸变化做回归；任何解码异常都不能发布半成品像素。
- [ ] (E2 阻塞) 在隔离探针中与参考结果比较合成画面、连续更新和尺寸，不向产品默认协商列表注册。
- [ ] (E2 阻塞) 许可证审查和安全审查通过后标记 E3 候选，保留未通过的真机项。

## 任务 5：实验性产品接入（E3 后，门禁阻塞 External Gate Blocked）

- [ ] (E3 阻塞) 为 planner/workflow 写失败测试：已验证能力可选择、未知能力不声明、服务端未选择时显示标准编码、认证失败不回退、取消释放、首选失败最多一条安全连接。
- [ ] (E3 阻塞) 加入显式实验性选择，不改变原有枚举值和保存默认值；若需要新持久化字段，使用正规迁移并测试旧库升级/回滚。
- [ ] (E3 阻塞) 请求编码与实际编码分开显示，首个 MVS 更新成功后才显示生效。
- [ ] (E3 阻塞) 运行期失败清理连接并标记该次恢复使用安全配置，避免自动重连重复选择已失败 MVS；不在损坏字节流里切换 decoder。
- [ ] (E3 阻塞) 在现有 diagnostics 加入闭集状态与有界统计，负向测试确保媒体载荷/秘密/地址/路径/异常原文不导出。
- [ ] (E3 阻塞) 回归键鼠、光标、剪贴板、尺寸变化和会话 ownership；任务审查通过后单独提交。

## 任务 6：真机验收与交付 (工程全量质量门禁完成，真机性能项保留 External Gate)

- [ ] (External Gate) 按规格比较标准/MVS 三类负载三轮，报告每轮及中位数；区分画质降低、分辨率改变与编码本身的贡献。
- [ ] (External Gate) 完成两小时稳定性、取消/重连/失败回退、输入四角和中心、窗口缩放与实际尺寸变化验证。
- [ ] (External Gate) 仅 E4 通过后开放自动优选；其他情况保留实验性并如实说明限制。策略尊重用户锁定，禁止自动黑白。
- [x] 运行完整回归、格式、Release x64、许可证/漏洞检查；对本轮新增依赖逐项核实分发条件，不绕过检查以获得绿灯。
- [x] 生成 ZIP/MSIX 并校验；确保不存在 RDM DLL、探针、捕获载荷或测试程序集。
- [x] 更新 evidence、README、兼容矩阵和发布记录。报告明确研究/实现/启用所处阶段，未完成真机项保留 External gate。

```powershell
dotnet restore WinARD.sln -p:Platform=x64
dotnet build WinARD.sln -c Release -p:Platform=x64 --no-restore -warnaserror
dotnet test WinARD.sln -c Release -p:Platform=x64 --no-build --no-restore -m:1
dotnet format WinARD.sln --verify-no-changes --no-restore
pwsh -File packaging/check-licenses.ps1
pwsh -File packaging/check-vulnerabilities.ps1
git diff --check
pwsh -File packaging/portable.ps1 -Version 0.1.0.0
pwsh -File packaging/verify-artifacts.ps1 -ExpectedVersion 0.1.0.0
```

## 早期执行记录：证据差距（已由顶部交接状态更新）

当前 E0 已满足，任务 1 完成，任务 2 & 3 已执行规范出口并完成缺失证据归档。E1/E2 协议契约不满足，严格执行 Spec 铁律停止解码实现；任务 4（解码实现）与任务 5（产品接入）保持 External Gate 阻塞。WinARD 保持标准 Zlib/ZRLE 安全编码路径。

## 早期执行记录与阻塞处理

每个任务完成时记录提交、命令、退出码、测试数量和证据文件索引。研究结果分别使用“观察到”“未观察到”“明确拒绝”“采集失败”，不得以未观察到替代明确拒绝。

| 任务 | 状态 | 关键判定与证据索引 |
|---|---|---|
| 任务 1 | 已完成 (Passed) | `RdmCaptureComparer` 重构并通过 63 个测试；`docs/testing/ard-mvs-protocol-capture.md` 更新两组单变量对比。 |
| 任务 2 | 已完成审计 (Gap Recorded) | RDM 2026.2.17.0 缺乏实际协商遥测出口；缺乏脱敏合成图真机环境；记录未观察到真实协商，保持 E0。 |
| 任务 3 | 已完成 E2 审查 (Blocked) | Wire ID、边界、算法、生命周期与像素语义缺失；触发 Spec 出口，输出 `docs/protocol/ard-mvs-evidence.md` 证据不足报告。 |
| 任务 4 | 门禁阻塞 (External Gate) | E2 不满足；严禁预建空 MVS decoder 或预填未知 ID；解码实现依法停止。 |
| 任务 5 | 门禁阻塞 (External Gate) | E3 不满足；产品选项保持关闭；保持标准 Zlib/ZRLE 协商与单一回退预算。 |
| 任务 6 | 工程校验完成 (Passed) | 零警告构建、全量回归通过（2458 测试）、格式规范通过、许可证/漏洞检查通过、打包产物验证通过。 |

| 条件 | 继续方式 |
|---|---|
| RDM 没有实际 codec 指标 | 从可验证的真实协商证据继续；性能对比只能作为收益观察，不单独满足 E1 |
| 模拟服务端握手失败或未到首帧请求 | 修复已定位的模拟兼容问题；不得把失败声明当作正常 RDM 协商 |
| 无法取得解密后的完整消息边界 | 输出缺失证据与可行采集入口；停止解码实现，保留标准编码路径 |
| 源码/组件许可不允许所需分发 | 使用协议事实独立实现或许可适用的系统能力；不能复制该组件进入发布包 |
| 真机临时不可用 | 完成已有契约支持的离线测试；真机结果保持 External gate |
| MVS 真机可用但收益不稳定 | 保留显式实验性选择；不默认开启自动优选 |

执行顺序不要求用户反复提供已保存的地址和密码。只有缺少设备选择、解锁/临时秘密或需要合成画面时，才通过对应界面取得必要输入；单纯文档与离线测试继续推进。
